{
  "name": "claude-monitor",
  "version": "0.2.0",
  "description": "Claude Code oturum monitoru — oturum bazli satirlar, gercek basliklar (fork: ekrtp, upstream: ibrahimokdadov/claudeMonitor)",
  "main": "monitor.js",
  "bin": { "claude-monitor": "monitor.js" },
  "scripts": {
    "start": "node monitor.js",
    "once": "node monitor.js --once",
    "all": "node monitor.js --once --all",
    "test": "node test/run.js",
    "probe": "node scripts/probe.js"
  },
  "dependencies": {},
  "engines": { "node": ">=18" },
  "os": ["win32"],
  "private": true
}
